// corner cases 1)when array is in the ascending order -->last element is the peak _Get_element_type
//              2)when array is in the decendind order -->first element is the peak _Get_element_type
//              3)when all element is same then --------->all element is the peak _Get_element_type

//              arr[]={12,20,15,2,40,90,65}

//              In the given array 20 and 90 are the peak element but we have to return only the 1 peak element

// using bianry search to find and apply it to the indices of array

// METHOD
// 1) do a linear search and check neghbour element if it less then the present element element or not
// order of o(n)

// 2)divide and conquer method
//  we apply the binary search and compare it to its neighbour 
//  if it is the highest from its neighbour then we will return it

#include<iostream>
using namespace std;

int main()
{
    
    return 0;
}